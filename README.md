# Introduction-to-Python-Finding-the-Largest-of-Three-Numbers
This Python program takes three user inputs and uses int() type casting to convert them into integers. It then applies nested conditional control structures (if–else) to compare the values and determine which number is the largest, finally displaying the highest value as the output.
